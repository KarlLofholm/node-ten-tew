<!DOCTYPE html>
<html>

<head>
    <title>Getting Started</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.1.4/semantic.min.css"
    />
    <link rel="stylesheet" type="text/css" href="/stylesheets/main.css" />
</head>

<body>
    <div class="wrapper">
        <header>
            <h1 class="ui center aligned header">Showing 10 latest tweets for the hashtag #<%= tweetQuery %></h1>
        </header>
        <div class="ui text container">
            <div class="ui items">
                <% for(var i=0; i<tweets.length; i++) {%>
                    <div class="item">
                        <a class="ui tiny image">
                <img src="<%= tweets[i].user.profile_image_url %>">
            </a>
                        <div class="content">
                            <a class="header">
                                <%= tweets[i].user.name %>
                            </a>
                            <div class="description">
                                <%= tweets[i].text %>
                            </div>
                        </div>
                    </div>
                    <% } %>
            </div>
        </div>
    </div>
</body>

</html>
